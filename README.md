# LogMachine, based on SQLite, is a general purpose, logging server, which also serves as the persistence layer for SitelineOS. LogMachine provides clients with a rich api. Documentation under development. 
